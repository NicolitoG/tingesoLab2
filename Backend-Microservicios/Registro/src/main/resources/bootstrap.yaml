server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  application:
    name: Registro
  datasource:
    url: jdbc:postgresql://registro-db-service:5432/registro-db
    username: postgres
    password: niko2003
  jpa:
    hibernate:
      ddl-auto: update