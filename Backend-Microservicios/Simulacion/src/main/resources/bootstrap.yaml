server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  application:
    name: Simulacion
  datasource:
    url: jdbc:postgresql://localhost:5433/microserviceSimulation
    username: postgres
    password: niko2003
  jpa:
    hibernate:
      ddl-auto: update