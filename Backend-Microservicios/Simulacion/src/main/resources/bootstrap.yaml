server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  application:
    name: Simulacion
  datasource:
    url: jdbc:postgresql://simulacion-db-service:5433/simulacion-db
    username: postgres
    password: niko2003
  jpa:
    hibernate:
      ddl-auto: update